# Default CI: Maven verify with Docker available for Testcontainers (Phase 1a test-plan).
# See README — Continuous integration.

name: CI

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
          cache: maven

      # Hosted runners expose Docker; Testcontainers needs a working daemon (Phase 1a test-plan / Story 18).
      - name: Docker available for Testcontainers
        run: docker version

      - name: Maven verify
        run: mvn --batch-mode verify
